{% extends 'base.twig' %}

{% block instructions %}
  <p>
  </p>
{% endblock %}

{% block content %}
  <div class="row">
    <p class="label">Descriptif général du projet :</p>
    <p id="description" class="value">{{ data.description }}</p>
  </div>

  <div class="row">
    <p class="label">Impact sur l'organisation des services :</p>
    <p id="impact" class="value">{{ data.impact }}</p>
  </div>

  <div class="row">
    <p class="label">Délai de réalisation :</p>
    <p id="delay" class="value">{{ data.delay }}</p>
  </div>

  <div class="row">
    <p class="label">Consultation du personnel :</p>
    <p id="staff-consulted" class="value">{{ data.staffConsulted ? 'Oui': 'Non' }}</p>
  </div>

{% endblock %}
